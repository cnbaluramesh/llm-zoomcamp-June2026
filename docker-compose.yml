services:
  app:
    build: .
    container_name: spacequest-app
    env_file: .env
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    ports:
      - "${APP_PORT:-8501}:8501"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
